# Stream-retention period < 24h — Loki's Limits.Validate() rejects this.
apiVersion: runtimeoverrides.io/v1alpha1
kind: LokiTenantOverride
metadata:
  name: bad-semantic
  namespace: test-tenant-a
spec:
  overrides:
    retention_stream:
      - selector: '{foo="bar"}'
        period: 12h
